services:
  app:
    image: customer-app:1.0
    container_name: customer-app-prod
    restart: unless-stopped
    environment:
      APP_VERSION: "1.0"
      APP_ENVIRONMENT: PRODUCTION
      DB_HOST: customer-db-prod
      POSTGRES_DB: customer
      POSTGRES_USER: customer
      POSTGRES_PASSWORD: customer123
    depends_on:
      db: {condition: service_healthy}
    ports: ["8083:8080"]
    networks: [customer-prod-net]
  db:
    image: postgres:16-alpine
    container_name: customer-db-prod
    restart: unless-stopped
    environment:
      POSTGRES_DB: customer
      POSTGRES_USER: customer
      POSTGRES_PASSWORD: customer123
    volumes: [customer-db-prod-data:/var/lib/postgresql/data]
    networks: [customer-prod-net]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U customer -d customer"]
      interval: 5s
      timeout: 5s
      retries: 20
networks:
  customer-prod-net: {name: customer-prod-net, driver: bridge}
volumes:
  customer-db-prod-data: {name: customer-db-prod-data}
